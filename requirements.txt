# Pinned to match the analyst's validated environment (pandas 2.2.x).
# The metric code uses the legacy 'M' resample alias, which still works in
# 2.2.x (emits a suppressed FutureWarning) and is removed in pandas 3.0.
pandas>=2.2,<2.3
numpy>=1.26,<2.1
scipy>=1.11
yfinance>=0.2.40,<0.3
requests>=2.31
joblib>=1.3
openpyxl>=3.1
python-dateutil>=2.8
toml>=0.10
